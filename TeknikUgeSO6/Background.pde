/*
int i;
boolean reversed = false;

class Star {
  float x,y;
  float speed;
  
  
  
  Star () {
    
    if(reversed) {
      x = width;
      speed = -5;
    }
    else{
      x = 0;
      speed = 5;
    }
    y = random(height);
    
  }
  
  void display() {
    x = x+speed;
    fill(255,255,204,150);
    noStroke();
    ellipse(x,y,10,10);
    
    if(x > width && !reversed) {
      stars.remove(i);
    }
    else if(x<0 && reversed) {
      stars.remove(i);
    }
  }

}

void update() {
  stars.add(new Star());
  
}*/
